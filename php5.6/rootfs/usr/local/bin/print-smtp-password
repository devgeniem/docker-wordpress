#!/bin/bash
# This is so that we can use msmtp without /etc/msmtprc config file
# msmtp doesn't have --password option and it only has --passwordeval option
# We use this script so that we can use it in passwordeval
echo $SMTP_PASSWORD
